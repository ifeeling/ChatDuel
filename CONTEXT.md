# ChatDuel 领域词汇

ChatDuel 将一次提问发送给多个 AI 平台，并把各平台的回答保存到同一轮历史记录中。这里记录跨代码与文档统一使用的业务名称。

## Language

**回答收集任务**：
用户每发送一次问题后产生的一轮回答跟踪；它可以同时跟踪多个 AI 平台，直到各平台的回答成功保存、失败或超时。
_Avoid_：回答回填任务、响应抓取任务、轮询任务

**待发送问题**：
回答收集任务尚未结束时，由用户提交并等待按顺序发送的问题；它保留加入队列时的问题内容、附件和目标 AI。
_Avoid_：后续消息、排队消息、待办问题

**平台通信**：
ChatDuel 向 AI 平台发出操作并接收结果的统一过程，包括发送问题、读取回答、检查可用状态和取得会话地址。
_Avoid_：平台消息传输、AI 连接管理

**会话网址**：
AI 平台中可定位一段具体对话的完整网址；不同平台可以用不同方式取得它。
_Avoid_：会话 ID、当前位置、页面地址

**平台命令**：
ChatDuel 向某个 AI 平台官方页面发出的一项具体操作，包括发送问题、读取最新回答、检查会话状态和取得会话网址；命令的词汇与参数由单一出处定义，两条通信通道用统一信封携带。
_Avoid_：消息、指令、请求

**问题发送协调器**：
接管用户提交问题后的完整派发过程：判断立即发送还是转为待发送问题、向各目标 AI 平台投递、把发送与回答收集的结果映射为界面状态与提示；页面只通过提交入口与事件订阅和它交互。
_Avoid_：发送管理器、发送服务、发送编排器

**适配器工具包**：
被五个平台适配器共用的、平台无关的 DOM 行为集合；它不区分平台，各家适配器把自己的选择器与写值、触发、评分策略交给它执行，从而取代原先散落各文件的逐字副本。实现位于 `src/adapters/shared/`，决策与落地见 ADR-0005（候选 5，父 issue #34）。
_Avoid_：适配公共库、适配器基类、DOM 工具箱

**适配原语**：
适配器工具包里的单个平台无关 DOM 辅助能力（跨 frame 找元素、写值、附件挂载、发送确认轮询、诊断上报、登录探测、命令桥存活探测等），可由多个适配器以各自参数复用。原语族与文件对应见 `src/adapters/shared/` 与 ADR-0005。
_Avoid_：公共函数、工具方法、辅助函数

**提示词优化**：
用户对共享输入框当前问题发起的一次改写尝试，把简单描述改写为更详细具体的提示词；改写结果需经用户预览确认（可编辑或恢复原文）后才会替换问题内容，确认前不影响原问题。由服务器代理转发完成，决策见 ADR-0006。
_Avoid_：优化提示词功能、AI 改写、prompt 优化、提示词增强

**匿名装机标识**：
扩展安装时在本机随机生成、只保存在本机的标识符，随提示词优化请求一起发给服务器，用来核算当日已用次数；不关联账号或其他可识别信息，重装扩展会生成新的标识。
_Avoid_：装机 ID、clientId、用户 ID、设备指纹

**发送前风险扫描**：
问题发送协调器在实际发送前，对待发送内容做的本地规则扫描（已知密钥格式前缀、证件号/银行卡校验位算法、高信息熵字符串+关键词上下文），用来提醒用户可能贴入了密钥、证件号等结构化敏感信息；不联网核对内容，也因此只能识别结构化敏感信息，识别不了没有固定格式的机密文字。决策见 [ai-arena-extension 全方位对比调研](docs/research/2026-08-21-ai-arena-extension-full-feature-comparison.md)，落地追踪 [issue #12](https://github.com/ifeeling/ChatDuel-extension-private/issues/12)。
_Avoid_：敏感信息检测、Gatekeeper、内容审核

**响应游标**：
适配器在发送前记住的候选回答节点位置（当前候选列表的最后一个元素），用来在读取时只看这个位置之后新出现的候选，从而判断"这是不是新回答"——用位置代替猜文本特征/前缀。目前在 DeepSeek、豆包两个平台试点，实现位于 `src/adapters/shared/response-cursor.ts`，决策与推广标准见 ADR-0008（issue #9）。
_Avoid_：候选打分、发送前快照、候选基线

**平台就绪体检**：
用户可手动触发的、对每个正在显示的平台并行做的三项只读检查——iframe 是否成功加载目标站点（未被拦截、未跳到网络错误页）、content-script 命令桥是否当下可响应（ping 原语）、是否可能未登录（登录探测扩展到全部 5 个平台）；三项结果各自独立展示，登录问题需要人工去登录，不计入"一键修复"的自动修复范围。实现位于 `src/chat/platform-health-check.ts`（体检本体）与 `src/adapters/shared/login-probe.ts`（登录探测原语），落地追踪 [issue #13](https://github.com/ifeeling/ChatDuel-extension-private/issues/13)。
_Avoid_：健康检查、诊断面板、连通性检测
