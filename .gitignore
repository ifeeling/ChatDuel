node_modules/
dist/
*.log
.DS_Store
.claude
.vscode/
coverage/
playwright-report/
test-results/
.worktrees/
.superpowers/
docs
*.zip

# 环境变量 / 密钥(防止误提交)。.env.development 和 .env.production
# 只装非敏感的地址常量,不在此列,允许提交。
.env
.env.local

# 测试目录(仅本地保留，不随仓库发布)
tests
AGENTS.md

# 本机调试信息(含服务器/数据库访问方法, 切勿提交)
.local

# 应用市场发布与素材脚本(仅本机使用, 不提交)
scripts
